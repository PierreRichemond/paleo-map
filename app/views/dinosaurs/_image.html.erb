<% if @dino.present? %>
  <%= image_tag(@dino.photo, style: "width: 100%;", id: "image") %>
<% else %>
  <% dino = @dinos.find { |d| d.photo.present? }  %>
  <%= image_tag(dino.photo, style: "width: 100%;", id: "image") %>
<% end %>
